import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  base: './', // Permite que los assets funcionen en GitHub Pages (subrutas) y en cualquier hosting
});
